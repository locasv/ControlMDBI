@* @model ControlMDBI.Models.AprobacionVehiculo

@{
    ViewData["Title"] = "Editar Aprobación";
    var solicitudSeleccionada = ViewData["SolicitudSeleccionada"] as ControlMDBI.Models.SolicitudVehiculo;
}

<div class="container-fluid">
    <h2 class="mt-4">Editar Aprobación de Solicitud</h2>
    <hr />

    <div class="row">
        <div class="col-md-6">
            <form asp-action="Edit">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <input type="hidden" asp-for="IdAprobacionVehiculo" />

                <div class="card mb-4">
                    <div class="card-header py-3">
                        <h6 class="m-0 font-weight-bold text-primary">Datos de la solicitud</h6>
                    </div>
                    <div class="card-body">
                        <div class="form-group">
                            <label asp-for="IdSolicitudVehiculo" class="control-label">Solicitud de Vehículo</label>
                            <select asp-for="IdSolicitudVehiculo" class="form-control" asp-items="ViewBag.IdSolicitudVehiculo" disabled></select>
                            <input type="hidden" asp-for="IdSolicitudVehiculo" />
                            <span asp-validation-for="IdSolicitudVehiculo" class="text-danger"></span>
                        </div>

                        @if (solicitudSeleccionada != null)
                        {
                            <div class="mt-4">
                                <div class="alert alert-info">
                                    <h6>Detalles de la solicitud:</h6>
                                    <hr />
                                    <div class="row">
                                        <div class="col-md-6">
                                            <p><strong>Solicitante:</strong> @solicitudSeleccionada.Usuario?.NombreUsuario</p>
                                            <p><strong>Fecha de solicitud:</strong> @solicitudSeleccionada.FechaSolicitud.ToString("dd/MM/yyyy HH:mm")</p>
                                            <p><strong>Recorrido : </strong> @solicitudSeleccionada.Recorrido</p>
                                        </div>
                                        <div class="col-md-6">
                                            <p><strong>Vehículo:</strong> @(solicitudSeleccionada.Vehiculo?.Marca + " " + solicitudSeleccionada.Vehiculo?.Marca)</p>
                                            <p><strong>Fecha salida:</strong> @solicitudSeleccionada.FechaSalida.ToString("dd/MM/yyyy HH:mm")</p>
                                            <p><strong>Fecha regreso:</strong> @solicitudSeleccionada.FechaRegreso.ToString("dd/MM/yyyy HH:mm")</p>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        }
                    </div>
                </div>

                <div class="card shadow mb-4">
                    <div class="card-header py-3">
                        <h6 class="m-0 font-weight-bold text-primary">Datos de la aprobación</h6>
                    </div>
                    <div class="card-body">
                        <div class="form-group">
                            <label asp-for="IdUsuario" class="control-label">Usuario que aprueba</label>
                            <select asp-for="IdUsuario" class="form-control" asp-items="ViewBag.IdUsuario"></select>
                            <span asp-validation-for="IdUsuario" class="text-danger"></span>
                        </div>

                        <div class="form-group">
                            <label asp-for="FechaAprobacion" class="control-label">Fecha de aprobación</label>
                            <input asp-for="FechaAprobacion" class="form-control" type="datetime-local" />
                            <span asp-validation-for="FechaAprobacion" class="text-danger"></span>
                        </div>

                        <div class="form-group">
                            <label asp-for="Estado" class="control-label">Estado</label>
                            <select asp-for="Estado" class="form-control" asp-items="@(new SelectList(Enum.GetNames(typeof(ListaEstadoAprobacion))))">
                                <option value="" selected disabled>-- Seleccione Estado --</option>
                            </select>
                            <span asp-validation-for="Estado" class="text-danger"></span>
                        </div>

                        <div class="form-group">
                            <label asp-for="Observaciones" class="control-label">Observaciones</label>
                            <textarea asp-for="Observaciones" class="form-control" rows="4"></textarea>
                            <span asp-validation-for="Observaciones" class="text-danger"></span>
                        </div>
                    </div>
                </div>

                <div class="form-group">
                    <button type="submit" class="btn btn-primary">
                        <i class="fas fa-save"></i> Guardar cambios
                    </button>
                    <a asp-action="Index" class="btn btn-secondary">
                        <i class="fas fa-arrow-left"></i> Volver a la lista
                    </a>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }

    <script>
        $(document).ready(function () {
            // Formatear fecha para el input datetime-local si es necesario
            var fechaAprobacion = $('#FechaAprobacion').val();
            if (fechaAprobacion) {
                var fecha = new Date(fechaAprobacion);
                var formattedDate = fecha.toISOString().slice(0, 16);
                $('#FechaAprobacion').val(formattedDate);
            }
        });
    </script>
} *@

@model ControlMDBI.Models.AprobacionVehiculo
@{
    ViewData["Title"] = "Editar Aprobación";
    var solicitudSeleccionada = ViewData["SolicitudSeleccionada"] as ControlMDBI.Models.SolicitudVehiculo;
}

<h1 class="mb-4">Editar Aprobación</h1>

<form asp-action="Edit">
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
    <input type="hidden" asp-for="IdAprobacionVehiculo" />
    <input type="hidden" asp-for="IdSolicitudVehiculo" />

    <div class="row">
        <!-- Datos de la solicitud -->
        <div class="col-md-6">
            <div class="card mb-4">
                <div class="card-header py-3">
                    <h5 class="m-0 font-weight-bold text-white">Datos de la solicitud</h5>
                </div>
                <div class="card-body">
                    <div class="form-group">
                        <label asp-for="IdSolicitudVehiculo" class="control-label">Solicitud de Vehículo</label>
                        <select asp-for="IdSolicitudVehiculo" class="form-control" asp-items="ViewBag.IdSolicitudVehiculo" disabled></select>
                        <span asp-validation-for="IdSolicitudVehiculo" class="text-danger"></span>
                    </div>

                    @if (solicitudSeleccionada != null)
                    {
                        <div class="alert alert-info mt-4">
                            <h6>Detalles de la solicitud:</h6>
                            <hr />
                            <div class="row">
                                <div class="col-md-6">
                                    <p><strong>Solicitante:</strong> @solicitudSeleccionada.Usuario?.NombreUsuario</p>
                                    <p><strong>Fecha de solicitud:</strong> @solicitudSeleccionada.FechaSolicitud.ToString("dd/MM/yyyy HH:mm")</p>
                                    <p><strong>Recorrido:</strong> @solicitudSeleccionada.Recorrido</p>
                                </div>
                                <div class="col-md-6">
                                    <p class="alert alert-info"><strong>Vehículo <br />Marca:</strong> @(solicitudSeleccionada.Vehiculo?.Marca)<br /><strong> Placa :</strong> @(solicitudSeleccionada.Vehiculo?.Placa)</p>
                                    <p><strong>Fecha salida:</strong> @solicitudSeleccionada.FechaSalida.ToString("dd/MM/yyyy HH:mm")</p>
                                    <p><strong>Fecha regreso:</strong> @solicitudSeleccionada.FechaRegreso.ToString("dd/MM/yyyy HH:mm")</p>
                                </div>
                            </div>
                        </div>
                    }
                </div>
            </div>
        </div>

        <!-- Datos de la aprobación -->
        <div class="col-md-6">
            <div class="card mb-4">
                <div class="card-header py-3">
                    <h5 class="m-0 font-weight-bold text-white">Datos de la aprobación</h5>
                </div>
                <div class="card-body">
                    <div class="form-group">
                        <label asp-for="IdUsuario" class="control-label">Usuario que aprueba</label>
                        <select asp-for="IdUsuario" class="form-control" asp-items="ViewBag.IdUsuario"></select>
                        <span asp-validation-for="IdUsuario" class="text-danger"></span>
                    </div>

                    <div class="form-group">
                        <label asp-for="FechaAprobacion" class="control-label">Fecha de aprobación</label>
                        <input asp-for="FechaAprobacion" class="form-control" type="datetime-local" />
                        <span asp-validation-for="FechaAprobacion" class="text-danger"></span>
                    </div>

                    <div class="form-group">
                        <label asp-for="Estado" class="control-label">Estado</label>
                        <select asp-for="Estado" class="form-control" asp-items="@(new SelectList(Enum.GetNames(typeof(ListaEstadoAprobacion))))">
                            <option value="" selected disabled>-- Seleccione Estado --</option>
                        </select>
                        <span asp-validation-for="Estado" class="text-danger"></span>
                    </div>

                    <div class="form-group">
                        <label asp-for="Observaciones" class="control-label">Observaciones</label>
                        <textarea asp-for="Observaciones" class="form-control" rows="4"></textarea>
                        <span asp-validation-for="Observaciones" class="text-danger"></span>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Botones centrados -->
    <div class="text-center mt-4">
        <button type="submit" class="btn btn-primary mx-2">
            <i class="fas fa-save"></i> Guardar cambios
        </button>
        <a asp-action="Index" class="btn btn-secondary mx-2">
            <i class="fas fa-arrow-left"></i> Volver a la lista
        </a>
    </div>
</form>
@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }

    <script>
        $(document).ready(function () {
            // Formatear fecha para el input datetime-local si es necesario
            var fechaAprobacion = $('#FechaAprobacion').val();
            if (fechaAprobacion) {
                var fecha = new Date(fechaAprobacion);
                var formattedDate = fecha.toISOString().slice(0, 16);
                $('#FechaAprobacion').val(formattedDate);
            }
        });
    </script>
} 